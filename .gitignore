# ความลับ ห้ามขึ้น git เด็ดขาด repo นี้เป็น public
.env

# วิดีโอทดสอบ ใหญ่เกินกว่าจะเก็บใน git
*.mp4
*.mov

# น้ำหนักโมเดล YOLO โหลดใหม่ได้เองจาก ultralytics ตอนรันครั้งแรก
*.pt

# เอกสารอ้างอิง ไม่ใช่ส่วนหนึ่งของโค้ด
*.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# โมเดล ML ที่สร้างจากข้อมูลจริง สร้างใหม่ด้วย train_model.py
*.joblib

# บันทึกภายในของเรา ไม่ต้องขึ้น repo สาธารณะ เอกสารที่ส่งจริงอยู่ใน Mini Project/
docs/

# สคริปต์ทดสอบ ใช้ตอนพัฒนาในเครื่อง ไม่ต้องขึ้น repo
test_payload.py
test_ml_features.py

# ไฟล์สำรองที่แก้เอง
*.bak
